#!/usr/bin/env python
# -*- coding: utf-8 -*-
from __future__ import unicode_literals

language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install:
  - pip install .
# command to run tests
script: pytest
